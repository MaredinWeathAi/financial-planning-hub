# Core (always needed)
# No external dependencies for CSV parsing — uses stdlib

# PDF Invoice Generation
reportlab>=4.0

# QuickBooks Online API (for pushing transactions)
python-quickbooks>=0.9.12
intuit-oauth>=1.3.0

# IBKR Flex Query fetcher + general HTTP
requests>=2.28.0

# Plaid (optional — for automated Bank of America data pull)
# plaid-python>=14.0.0

# Web application (Financial Planning Hub)
flask>=2.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0

# Business PDF import
pdfplumber>=0.9.0

# Scheduling (optional — for cron-like automation)
# schedule>=1.2.0
# APScheduler>=3.10.0
